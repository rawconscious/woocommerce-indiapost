import React from 'react';
import ReactDOM from 'react-dom';
import Invalid from './components/invalid';

const WcipInvalid = () => {

    return (
        <>
            <Invalid />
        </>
    );

};
ReactDOM.render(<WcipInvalid />, document.getElementById('wcip-react-app'));